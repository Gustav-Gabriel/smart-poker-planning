# Chave da API Tenor (obrigatória para busca de GIFs no avatar).
# Obtenha em https://developers.google.com/tenor
TENOR_API_KEY=

# Porta HTTP do servidor customizado (Next + Socket.io). Padrão: 3000.
PORT=3000
